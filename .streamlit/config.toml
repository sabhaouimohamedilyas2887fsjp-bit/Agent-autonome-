[browser]
gatherUsageStats = false

[server]
headless = true
port = 8501
fileWatcherType = "poll"

[theme]
primaryColor        = "#1565C0"   # bleu principal (boutons, liens actifs)
backgroundColor     = "#FFFFFF"   # fond blanc
secondaryBackgroundColor = "#E3F2FD"  # bleu très clair (sidebar, cards)
textColor           = "#0D1B2A"   # texte quasi-noir
font                = "sans serif"
